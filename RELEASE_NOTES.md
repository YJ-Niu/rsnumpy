# Release Notes

- python 可以使用 `pip install rsnumpy` 安装，也可以使用 `uv pip install rsnumpy` 安装

## v1.2.1 (2026-07-28)

### 性能优化

- **矩阵求逆加速**：Rust 层 2×2 批量矩阵求逆新增 rayon 并行加速，支持复数类型；Python 层 `linalg.inv` 的 2×2 批量分支优先调用并行 Rust 实现
- **阻抗重归一化加速**：重构 2 端口阻抗重归一化逻辑，支持每频点独立阻抗参数；标量阻抗快速路径避免两次矩阵求逆，加速约 700 倍
- **S 参数求逆快速路径**：2 端口 S 参数求逆新增闭式快速计算路径，加速约 8 倍

### Bug 修复

- 修复 skrf 中 S 参数矩阵构造的 dtype 推断问题（显式指定 `dtype=complex`）
- 抑制 CPW 导体损耗计算的低频 RuntimeWarning

### 重构

- 为 2 端口网络 `renormalize_s` 添加标量阻抗快速路径
- 修复 cpw.py 中的代码换行格式问题
- 新增 CPW 相关测试代码

## v1.2.0 (2026-07-24)

### 代码清理

- 删除大量冗余的测试和废弃代码文件，精简项目体积

## v1.1.9 (2026-07-23)

### Bug 修复

- 修复 `recfunctions` 逻辑问题

### 重构

- 重命名核心模块并调整导入路径
- 调整 `__init__.py` 导入顺序避免循环导入

### 测试增强

- 批量新增测试文件与模块初始化代码

## v1.1.8 (2026-07-21)

### Bug 修复

- 修复复数类型相关的几个函数逻辑

### 重构

- 整理代码格式并修复多处逻辑问题

### 测试增强

- 调整史密斯图线条宽度并新增电感测试案例

## v1.1.6 (2026-07-11)

### 性能架构

- **主动释放 GIL**：`sort`、`argsort`、`cumsum`、`cumprod` 及逐元素、归约、矩阵乘等计算密集算子在进入 Rust 前通过 `py.detach` 释放 GIL，与其它 Python 线程真正并行
- **成本分级并行阈值**：按每元素成本拆分阈值——超越函数（`sin`/`exp`）最低、访存密集型（`add`/`mul`/`div`）最高——避免小数组因线程调度反而变慢
- **`cumsum`/`cumprod` 重写**：单份 C 序缓冲就地前缀扫描 + 连续块并行，`axis=None` 走展平快速路径，中大规模较 NumPy 快约 1.9x
- **BLAS 后端**：macOS 上 `matmul`/`dot` 经系统 Accelerate 框架分派，与 NumPy 基本持平

### 功能增强

- 新增 `frexp`、`i0`、`convolve`、`correlate`、`interp` 等 Rust 原生实现
- 重构 Python 侧冗余实现，统一走 Rust 后端计算逻辑，并拆分公共工具函数模块

### 构建优化

- 关闭 `zip` 等依赖中未使用的编解码器特性（zstd/lzma/bzip2 等）并移除 ndarray 未用的 `serde` 特性，配合 `lto = "fat"` 与符号裁剪，扩展体积减小约 14%（约 5 MB）

### 文档更新

- README 性能对比表更新为真实实测数据，新增「性能架构」小节；修复英文项目结构树，统一 Python 版本要求为 ≥ 3.10

## v1.1.5 (2026-07-10)

### 功能增强

- **字符串 / void dtype 支持**：新增字符串与 void 数据类型
- **掩码数组与结构化数组**：新增掩码数组、结构化数组相关模块与工具函数

### Bug 修复

- 修复幂运算（`power`）操作符行为

## v1.1.4 (2026-07-10)

### 功能增强

- 新增 `masked_select` 函数

### 重构

- 重构数组格式化逻辑，实现多维缩进对齐
- 重构 IO 模块

## v1.1.3 (2026-07-09)

### 性能优化

- **逐元素运算并行 + GIL 释放**：为一元 / 二元逐元素运算引入并行阈值并释放 GIL
- 优化 BLAS、切片与线性代数实现

## v1.1.2 (2026-07-09)

### 功能增强

- `np.unique` 新增 `axis` 参数支持
- 新增布尔掩码索引与取模（`%`）运算符支持

## v1.1.1 (2026-07-09)

### 功能增强

- 更新 `reshape` API
- 新增集合运算（`intersect1d`、`union1d`、`setdiff1d` 等）

## v1.1.0 (2026-07-09)

### 功能增强

- **补充 API**：新增约 140 个 NumPy 兼容函数（别名、nan 系列、集合运算、窗函数、信号等），对齐 NumPy 2.5.1
- 改进 `transpose` 实现

## v1.0.8 (2026-07-09)

### 功能增强

- **日期时间支持**：新增 `datetime64` / `timedelta64` 标量与日期序列生成

### Bug 修复

- 修复随机数函数的返回类型

## v1.0.7 (2026-07-07)

### 功能增强

- **NumPy 数组接口协议**：实现 `__array__` 等接口，支持 matplotlib 直接绘图

### 性能优化

- 优化 `argmax` / `argmin` / `argsort` 实现逻辑

## v1.0.6 (2026-07-06)

### 功能增强

- 支持数组接口协议，新增字符串数组支持并优化内置函数替换

### Bug 修复

- 修复多维索引赋值（multi-index assignment）问题

### 构建优化

- 构建前新增 `cargo fmt` 静态检查

## v1.0.4 (2026-07-03)

### 构建 / CI

- 新增 `rust-toolchain.toml` 固定工具链版本
- 更新 pyo3 依赖格式与特性声明
- 优化发布工作流（maturin 1.14.1），整理 pyproject 分类信息

## v1.0.3 (2026-07-03)

### 依赖 / 构建

- 升级 `rand` 至 0.10.2
- 调整 ndarray 特性（此版本暂移除 BLAS 支持，后于 v1.1.3 重新优化引入）

### 重构

- 重构随机数生成模块，提升可读性与一致性

### CI

- 更新 CI 配置，新增 `PYTHONUTF8` / `PYTHONIOENCODING` 环境变量

## v1.0.2 (2026-06-24)

### 性能优化

- **索引模块优化**：向量预分配，提升花式索引性能
- **数组拼接优化**：`concatenate`、`stack` 等函数性能提升
- **数组格式化输出优化**：预分配字符串缓冲区、简化切片遍历逻辑
- **工具函数优化**：`arange`、`reshape` 等函数的预分配和边界处理
- **IO 函数优化**：`loadtxt`、`savez` 等函数的预分配和遍历逻辑

### 模块重构

- **随机数模块重构**：使用 const 初始化线程局部 RNG、优化形状解析预分配、使用 `div_ceil` 计算分块大小、简化采样类型转换
- **线性代数模块重构**：将 Python 绑定函数移至 PyO3 detach 闭包，避免 GIL 持有过长时间
- **统计函数优化**：简化 `polyfit`、`cov` 等计算的循环逻辑

### Bug 修复

- 修复 `median`、`percentile` 等统计函数的偶数长度判断逻辑
- 修复部分边界检查的冗余代码

### 构建优化

- 新增 `build_wheel.sh` 自动同步版本号的逻辑
- 优化构建配置，二进制大小略有优化

## v1.0.1 (2026-06-24)

### 功能增强

- **新增 ArrayFlags 支持**：提供数组标志位查询功能
- **新增 like-series 数组创建函数**：支持根据已有数组的属性创建新数组
- **改进 logspace 函数**：优化对数空间生成算法

### 性能提升

- **并行加速矩阵运算**：利用多线程并行化矩阵运算
- **随机数生成优化**：并行加速随机数生成
- **构建配置优化**：调整编译参数，提升构建效率

### 代码重构

- **迁移矩阵和数组操作到 Rust 后端**：将大量矩阵和数组操作从 Python 层迁移到 Rust 层实现
- **重构 matlib.py 的矩阵函数实现**：优化矩阵函数的代码结构

### 文档更新

- 更新 README 中 Python 版本支持要求至 3.10+
- 更新 README 文档细节并修复格式问题

---

## v1.0.0 (Initial Release)

### 核心功能

- **高性能多维数组**：基于 Rust 的 ndarray 实现，提供 NumPy 兼容的 API
- **数学函数**：支持 sin、cos、exp、sqrt、log 等常见数学函数
- **数组操作**：支持 reshape、transpose、切片、拼接等操作
- **统计函数**：支持 sum、mean、std、var、max、min、argmin、argmax 等
- **线性代数**：支持 det、inv、norm、solve、svd、qr 等矩阵运算
- **FFT**：支持快速傅里叶变换及其逆变换
- **随机数**：支持 uniform、normal、integers 等分布，兼容新旧两种 API
- **多项式**：支持多项式求值、求根、运算和拟合
- **文件 I/O**：支持 .npy、.npz 和文本文件的读写

### 优势

- **性能**：核心计算使用 Rust 实现，部分操作（sum、mean、dot 等）比 NumPy 快 1.2x-2.0x
- **类型安全**：利用 Rust 的强类型系统避免运行时错误
- **内存安全**：无数据竞争，无内存泄漏
- **完整 API**：覆盖 NumPy 常用功能

### 支持平台

- Python ≥ 3.10
- Rust ≥ 1.75
- macOS、Linux、Windows
